   /* 

   "nodemon": restart server on save
   "express": node framework
   "dotenv": environments variables

   "cookie-parser": parse cookies

   "date-fns": dates format
   "uuid": uriversal unique identifier

   "cors": Cross-Origin Resource Sharing - allow web applications to make requests to a different domain, we can configure the requested domain in config folder


   "mongoose": database
   "mongoose-sequence": Mongoose plugin


   "bcrypt": password encryption
   "express-async-handler": error handling, 
   automatically catches any errors that are thrown in the async code. If an error occurs, it will call the next middleware
   It helps you avoid manually writing try-catch blocks in each route handler for handling async errors.


*/